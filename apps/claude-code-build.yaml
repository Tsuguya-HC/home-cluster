apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: claude-code-build
  namespace: argocd
spec:
  project: argo
  source:
    repoURL: https://github.com/Tsuguya-HC/home-cluster.git
    targetRevision: main
    path: manifests/claude-code-build
  destination:
    server: https://kubernetes.default.svc
    namespace: claude-code-build
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
nameSuffix: -tests
patches:
  - target:
      kind: TaskHandler
    patch: |-
      - op: replace
        path: /spec/phase
        value: tests

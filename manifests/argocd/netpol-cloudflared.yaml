apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: cloudflared
  namespace: argocd
spec:
  endpointSelector:
    matchLabels:
      app: cloudflared
  # **`ingressDeny` に `cluster` を書かないこと。** `cluster` は host と remote-node を
  # 含み、deny は allow に優先するので kubelet の probe まで落ちる。probe がある Pod は
  # `ingressDeny` を使わず、ingress の allow だけ書く（書かない先は暗黙で deny される）。
  ingressDeny:
    - fromEntities:
        - world
  egress:
    - toFQDNs:
        - matchPattern: "*.v2.argotunnel.com"
        - matchName: "cftunnel.com"
        - matchName: "h2.cftunnel.com"
        - matchName: "quic.cftunnel.com"
      toPorts:
        - ports:
            - port: "443"
              protocol: TCP
            - port: "7844"
              protocol: TCP
            - port: "7844"
              protocol: UDP
    - toEndpoints:
        - matchLabels:
            app.kubernetes.io/name: argocd-server
      toPorts:
        - ports:
            - port: "8080"
              protocol: TCP
    - toEndpoints:
        - matchLabels:
            eventsource-name: github-webhook
            io.kubernetes.pod.namespace: argo
      toPorts:
        - ports:
            - port: "12000"
              protocol: TCP
    - toEndpoints:
        - matchLabels:
            app.kubernetes.io/name: kanidm
            io.kubernetes.pod.namespace: kanidm
      toPorts:
        - ports:
            - port: "8443"
              protocol: TCP
    - toEndpoints:
        - matchLabels:
            app.kubernetes.io/name: nextcloud
            io.kubernetes.pod.namespace: nextcloud
      toPorts:
        - ports:
            - port: "80"
              protocol: TCP
    - toEndpoints:
        - matchLabels:
            app: harbor
            component: nginx
            io.kubernetes.pod.namespace: harbor
      toPorts:
        - ports:
            - port: "8443"
              protocol: TCP
    - toEndpoints:
        - matchLabels:
            app.kubernetes.io/instance: oauth2-proxy-rss
            io.kubernetes.pod.namespace: oauth2-proxy
      toPorts:
        - ports:
            - port: "4180"
              protocol: TCP
